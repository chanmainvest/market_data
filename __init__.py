"""market_data scrapers package.

Scripts in this directory are also runnable directly (``uv run python
market_data/scrap_yahoo_daily_close.py``) since they use flat imports
(``from scrap_utils import *``, ``import db``) that resolve to siblings.
"""
